## Arrays:


### الجزء الأول: الـ 1D Array (أساسيات الميموري المتصلة)

في الجزء ده هنفهم يعني إيه "Contiguous Memory" وإزاي الـ Array name هو في الأصل Pointer.



```c
#include <iostream>
#include <cstdio> // عشان نستخدم printf لعرض العناوين بوضوح

using namespace std;

void part1_basics() {
    printf("\n--- Part 1: 1D Array Basics & Memory ---\n");

    // 1. Declaration & Initialization
    // حجزنا 5 أماكن (4 بايت لكل مكان) ورا بعض في الـ Stack
    int arr[5] = {10, 20, 30, 40, 50}; 

    // 2. Accessing Elements (Traditional Way)
    printf("Value at index 2: %d\n", arr[2]); // هيطبع 30

    // 3. The "Mina Nagy" Way (Under the Hood)
    // اسم الـ Array هو Pointer لأول عنصر (Base Address)
    printf("Address of arr (Base Address): %p\n", (void*)arr);
    printf("Address of arr[0]:             %p\n", (void*)&arr[0]);
    
    // إثبات إنهم ورا بعض (Contiguous)
    // لاحظ الفرق في العناوين هيكون 4 بايت (حجم الـ int)
    printf("Address of arr[1]:             %p (Base + 4 bytes)\n", (void*)&arr[1]);

    // 4. Pointer Arithmetic (اللعب في الميموري)
    // *(arr + 2) هي هي arr[2]
    // معناها: روح للعنوان الأساسي، نط خطوتين (حجم الخطوة int)، وهات القيمة
    printf("Value using Pointer Math *(arr + 2): %d\n", *(arr + 2));
}
```

> [!TIP] قاعدة ذهبية
> 
> الـ arr[i] بيترجمها الكومبايلر لـ *(arr + i). عشان كده الـ Access Time هو O(1) لأنها عملية حسابية بسيطة مش بحث.

السطرين دول مهمين جدا --> 
```c
    printf("Address of arr (Base Address): %p\n", (void*)arr);
    printf("Address of arr[0]:             %p\n", (void*)&arr[0]);
```


### 1. النتيجة (The Output)

لو عملت Run للكود ده، هتلاقي السطرين طبعوا نفس الرقم بالظبط (نفس العنوان في الميموري).

مثلاً:



```Plaintext
Address of arr (Base Address): 0x7ffee4b5
Address of arr[0]:             0x7ffee4b5
```

---

### 2. التفسير التقني (Why?)

**السطر الأول:** `(void*)arr`

- في لغة C/C++، اسم الـ Array (`arr`) لوحده بيحصل له حاجة اسمها **Decay**.
    
- يعني "بيتحلل" ويبقى عبارة عن **Pointer لأول عنصر فيه**.
    
- كأنك بتقول للميموري: "قوليلي السلسلة دي بتبتدي منين؟".
    

**السطر الثاني:** `(void*)&arr[0]`

- هنا إنت بتجيبها "بلدي": بتروح لأول عنصر `arr[0]` وتقوله "هات عنوانك" بعلامة الـ `&`.
    
- بما إن أول عنصر هو بداية الـ Array، فالعنوان هو هو.
    

> الخلاصة:
> 
> arr $\equiv$ &arr[0]
> 
> اسم المصفوفة = عنوان أول عنصر فيها.

---

### 3. ليه استخدمنا `(void*)`؟ (Casting)

دي "نظافة كود" عشان الـ Compiler ميزعلش.

- الرمز `%p` في `printf` مخصص لطباعة العناوين، بس هو منتظر نوع محدد اسمه `void*` (عنوان عام).
    
- الـ `arr` نوعه `int*` (عنوان لرقم صحيح).
    
- فإحنا بنعمل **Casting** (تحويل نوع) عشان نقول للكومبايلر: "عامل العنوان ده كأنه عنوان عام واطبعهولي Hexadecimal".
    

---

### 💡 معلومة للمحترفين (Interview Trick)

هل معنى كده إن `arr` هو هو `&arr`؟

- **كقيمة (Value):** أه، هما نفس رقم العنوان.
    
- **كنوع (Type):** لأ!
    
    - `arr`: هو مؤشر لـ `int` (بيشاور على أول صندوق).
        
    - `&arr`: هو مؤشر لـ `Array كاملة` (بيشاور على البلوك كله).
        

لو زودت 1 على arr هينط 4 بايت (حجم int).

لو زودت 1 على &arr هينط 20 بايت (حجم الـ Array كلها)!

طب لو هستخدم cout؟
سؤال ممتاز! النقلة من `printf` لـ `cout` بتريحك كتير، لأن `cout` أذكى (Smart) وبتستخدم خاصية اسمها **Operator Overloading**.

هي بتشوف نوع الداتا إيه، وتتصرف بناءً عليه.

### 1. مع مصفوفات الأرقام (`int`, `double`...)

هنا **مش محتاج** تعمل `(void*)` ولا توجع دماغك.

- الـ `cout` لما بتشوف `int*` (عنوان رقم)، بتفهم لوحدها إنك عايز تطبع العنوان، فبتطبعه Hexadecimal أوتوماتيك.
    



```c
int arr[5] = {10, 20};

// C Style (القديم المتعب)
printf("%p\n", (void*)arr); 

// C++ Style (الذكي)
std::cout << arr << std::endl;      // بيطبع العنوان علطول
std::cout << &arr[0] << std::endl;  // نفس العنوان
```

---

### 2. "الفخ" الكبير في الـ `char` (مهم جداً!) 🚨

لو المصفوفة بتاعتك نوعها `char`، الـ `cout` هتتصرف بطريقة مختلفة تماماً.

- في C++، الـ `char*` معناه "String".
    
- فلو اديتها عنوان `char`، هي **مش هتطبع العنوان**، هي هتفضل تطبع الحروف اللي جواه لحد ما تلاقي `\0` (Null Terminator).
    

**عشان تجبرها تطبع عنوان الـ `char`، لازم ترجع تستخدم `(void*)` تاني.**

شوف المثال ده عشان تفهم الفرق:



```c
#include <iostream>

using namespace std;

int main() {
    char name[] = "Mina";

    // 1. الطبيعي بتاع cout مع الـ char (بتطبعه ككلمة)
    cout << "Value: " << name << endl;  // هيطبع: Mina

    // 2. لو حاولت تطبع العنوان من غير casting
    cout << "Address? " << &name[0] << endl; // برضه هيطبع: Mina (لأن ده char*)

    // 3. الحل: لازم تعمل Casting عشان تقولها "انسى إنه حرف، عامليه كعنوان مجرد"
    cout << "Real Address: " << (void*)name << endl; // هيطبع: 0x7ffe...
    
    return 0;
}
```

### الخلاصة (The Rule of Thumb):

1. مع أي `Array` عادية (`int`, `float`, `struct`): استخدم `cout << arr` علطول، هيطبع العنوان.
    
2. مع الـ `char Array` (Strings):
    
    - عايز الكلمة؟ `cout << arr`
        
    - عايز العنوان؟ `cout << (void*)arr`
        

---


### يعني إيه `ptr + 1`؟ (The Golden Rule)

في الرياضة العادية: $5 + 1 = 6$.

في لغة الـ C/C++: ptr + 1 مش معناها زود "واحد بايت" على العنوان.. لا!

المعنى الحقيقي: **"يا مؤشر، نط خطوة واحدة لقدام بحجم النوع اللي إنت شايله"**.

- لو المؤشر `char*` (نملة): الخطوة بـ **1 بايت**.
    
- لو المؤشر `int*` (إنسان): الخطوة بـ **4 بايت**.
    
- لو المؤشر `long long*` (فيل): الخطوة بـ **8 بايت**.
    

المعادلة اللي الـ CPU بيحسبها في الخلفية:

$$NewAddress = OldAddress + (n \times sizeof(DataType))$$

---

### كود الإثبات (عشان تصدق عينك)

خد الكود ده وشوف الـ Output بتاعه هيبهرك إزاي، بيوريك الفرق بين "خطوة النملة" و "خطوة الفيل":

C++

```
#include <iostream>
using namespace std;

int main() {
    // 1. Integer Pointer (Step = 4 bytes)
    int x = 10;
    int* iPtr = &x;
    cout << "Int Ptr Before: " << iPtr << endl;
    iPtr++; // خد خطوة
    cout << "Int Ptr After:  " << iPtr << " (Difference: 4 bytes)" << endl;
    
    cout << "------------------------" << endl;

    // 2. Char Pointer (Step = 1 byte)
    char c = 'A';
    char* cPtr = &c;
    // casting to void* force printing address not string
    cout << "Char Ptr Before: " << (void*)cPtr << endl; 
    cPtr++; // خد خطوة
    cout << "Char Ptr After:  " << (void*)cPtr << " (Difference: 1 byte)" << endl;

    cout << "------------------------" << endl;

    // 3. Long Long Pointer (Step = 8 bytes)
    long long l = 1000;
    long long* lPtr = &l;
    cout << "Long Ptr Before: " << lPtr << endl;
    lPtr++; // خد خطوة
    cout << "Long Ptr After:  " << lPtr << " (Difference: 8 bytes)" << endl;

    return 0;
}
```

> [!NOTE] الرسمة دي بتوضحلك
> 
> إن +1 دي زي "البلاطة". بلاطة الـ char صغيرة، وبلاطة الـ int كبيرة. الـ Pointer بيمشي بلاطة بلاطة مش سنتي سنتي.

---

### بهاريز الإنترفيوهات (The Void Pointer Trap) 🔥

سؤال خبيث جداً بييجي في الإنترفيوهات:

"يا بشمهندس، ينفع نعمل Arithmetic على void*؟"

C++

```
void* ptr;
ptr++; // ينفع ولا يضرب Error؟
```

**الإجابة النموذجية (بتاعة السينيرز):**

1. **حسب الـ Standard (ANSI C / ISO C++):**
    
    - **ممنوع!** (Compilation Error).
        
    - ليه؟ لأن `void` معناها "لا شيء"، يعني حجمه غير معروف. الكمبيوتر مش عارف ينط كام خطوة.
        
2. **حسب الـ GCC Compiler (اللي معظمنا شغال عليه):**
    
    - **بيسمح بيها** (كـ Extension من عنده) وبيعتبر حجمها **1 بايت** (زي الـ `char`).
        

> [!TIP] الخلاصة للنوتس بتاعتك
> 
> "Never do pointer arithmetic on a void* unless you cast it first."
> 
> عشان كودك يشتغل على كل الكومبايلرات، لازم تعمل Casting الأول:
> 
> ((char*)ptr)++

---

### التحدي الجاي (البعبع الحقيقي) 👻

إحنا كدة عرفنا نتحرك بالمؤشر (Navigation). الخطوة الجاية هي **"إزاي نمد إيدنا نجيب اللي جوه الصندوق"** أو ما يسمى بـ **Dereferencing `*ptr`**.

جاهز ندخل على عملية "السطو" على الميموري وتغيير القيم عن بعد؟ 😉

---

### الجزء الثاني: الـ 2D Array (المصفوفات)

هنا بقى الكلام الكبير. الـ 2D Array في الميموري هي في الحقيقة 1D Array طويلة، بس إحنا اللي بنتخيلها صفوف وأعمدة.



```c
void part2_2d_arrays() {
    printf("\n--- Part 2: 2D Arrays (Matrices) ---\n");

    // 1. Declaration: 3 Rows, 3 Columns
    // تخيلها كأنها Excel Sheet
    int matrix[3][3] = {
        {1, 2, 3},  // Row 0
        {4, 5, 6},  // Row 1
        {7, 8, 9}   // Row 2
    };

    // 2. Nested Loops (Iterating)
    // اللوب الخارجية للصفوف، والداخلية للأعمدة
    printf("Printing Matrix:\n");
    for (int i = 0; i < 3; i++) {       // Row Walker
        for (int j = 0; j < 3; j++) {   // Column Walker
            printf("%d ", matrix[i][j]);
        }
        printf("\n"); // انزل سطر جديد بعد كل صف
    }
}
```


---

### الجزء الثالث: الـ 2D Array في الميموري (Row-Major Order)

ده أهم جزء لمهندس الـ Embedded. لازم تعرف إن الكمبيوتر بيخزن الصف الأول كله، وبعدين الصف التاني وراه علطول.



```c
void part3_memory_layout() {
    printf("\n--- Part 3: 2D Array Memory Secrets ---\n");

    int matrix[2][3] = {
        {1, 2, 3}, 
        {4, 5, 6}
    };

    // تعال نشوف العناوين ورا بعض إزاي
    printf("Address of [0][0]: %p (Value: 1)\n", (void*)&matrix[0][0]);
    printf("Address of [0][1]: %p (Value: 2)\n", (void*)&matrix[0][1]);
    printf("Address of [0][2]: %p (Value: 3)\n", (void*)&matrix[0][2]);
    
    // المفاجأة هنا: أول عنصر في الصف التاني بييجي مباشرة بعد آخر عنصر في الصف الأول
    // ده بيثبت إن الـ 2D Array هي مجرد وهم، وفي الحقيقة هي Linear في الميموري
    printf("Address of [1][0]: %p (Value: 4) <- Contiguous!\n", (void*)&matrix[1][0]);
}
```

---

### كود التحدي (Challenge) 🚀

عشان تتأكد إنك هضمت الفكرة، خد الكود ده وحاول تكمله:

المطلوب:

برنامج يحسب مجموع كل صف لوحده (Row Sum).

C++

```
void challenge() {
    printf("\n--- Challenge: Row Sum Calculator ---\n");
    
    int data[3][4] = {
        {10, 10, 10, 10},
        {20, 20, 20, 20},
        {5,  5,  5,  5}
    };

    for (int i = 0; i < 3; i++) {
        int sum = 0;
        // TODO: اكتب كود هنا يجمع عناصر الصف الحالي (i)
        // Hint: You need an inner loop iterating from j = 0 to 4
        
        // .... كودك هنا ....

        printf("Sum of Row %d = %d\n", i, sum);
    }
}

int main() {
    part1_basics();
    part2_2d_arrays();
    part3_memory_layout();
    challenge();
    return 0;
}
```

### 🔗 روابط لمواضيع مرتبطة في نوتس أوبسيديان عندك:

- [[Pointer Arithmetic]]
    
- [[Contiguous Memory]]
    
- [[Row Major Order]]
    
- [[Nested Loops]]
    

جرب الكود ده وقولي لو "التحدي" وقف معاك!