أحلى "Cheat Sheet" ليك يا هندسة. ده الجدول "المنقذ" اللي هيلملك كل أنواع الـ Declarations اللي في المنهج (واللي برا المنهج كمان) في مكان واحد.

الجدول ده متقسم لـ 4 أقسام رئيسية: **الأساسيات**، **الـ Const**، **المصفوفات**، و **الدوال**.

💡 **قاعدة ذهبية قبل ما تقرأ:** عشان تفك طلاسم أي تعريف معقد، اقرأه **من اليمين للشمال** (Right-to-Left Rule)، وأدي الأولوية للأقواس `()`.

---

### 1. الأساسيات والخدع (Pointers & References Basics)

|**الشكل (Syntax)**|**بيتقري إزاي؟ (How to read)**|**المعنى (Meaning)**|**⚠️ تريكة الامتحان**|
|---|---|---|---|
|`int* p;`|Pointer to int|متغير شايل **عنوان** مكان في الميموري فيه رقم صحيح.|ممكن يبقى `nullptr` عادي.|
|`int& r = x;`|Reference to int|**اسم دلع** للمتغير `x`. هما الاتنين حاجة واحدة.|**لازم** ياخد قيمة وقت التعريف، ومستحيل يبقى `NULL`.|
|`int *p, q;`|p is ptr, q is int|`p` مؤشر، لكن `q` متغير `int` عادي!|خد بالك النجمة بتأثر على اللي لازقة فيه بس.|
|`int **ptr;`|Pointer to Pointer|مؤشر بيشاور على مؤشر تاني. (سلسلة عناوين).|بيستخدم عشان تعمل 2D Array ديناميك.|
|`void* p;`|Void Pointer|مؤشر "عام" بيشاور على أي حاجة (int, float, char).|مينفعش تعمل `*p` (Dereference) غير لما تعمله Cast الأول.|

---

### 2. عقدة الـ Const (اقرأ من اليمين للشمال ⬅️)

الامتحانات بتحب دي جداً عشان يشوفك فاهم مين اللي ثابت.

|**الشكل (Syntax)**|**الترجمة (من اليمين ⬅️)**|**المعنى بالبلدي**|**الخلاصة**|
|---|---|---|---|
|`const int* p;`|p is a pointer to `const int`|المؤشر حر (يتحرك ويشاور على غيره)، بس **الرقم اللي جوه الصندوق ممنوع يتغير**.|Read-only Data|
|`int const* p;`|(نفس اللي فوق بالظبط)|نفس المعنى (المكان متغير).|Read-only Data|
|`int* const p;`|p is a `const` pointer to int|**المؤشر ملحوم في مكانه** (ممنوع يشاور على غيره)، بس الرقم اللي جوه ممكن يتغير.|Fixed Address|
|`const int* const p;`|Const ptr to const int|سجن مشدد! لا المؤشر بيتحرك، ولا الرقم بيتغير.|Totally Constant|

---

### 3. وحش المصفوفات (Arrays & Pointers Combinations)

دي أصعب حتة في السنتكس، ركز في مكان **الأقواس `()`**.

|**الشكل (Syntax)**|**النوع**|**المعنى والتخيل**|**مثال الاستخدام**|
|---|---|---|---|
|`int* arr[5];`|**Array of Pointers**|مصفوفة عادية فيها 5 خانات، بس كل خانة شايلة **مؤشر** (عنوان).|`char* names[] = {"Ali", "Mona"};`|
|`int (*ptr)[5];`|**Pointer to Array**|ده **مؤشر واحد بس**، بيشاور على "صف كامل" أو مصفوفة كاملة حجمها 5.|لما تشاور على صف في `2D Array`.|
|`int arr[5];`|Normal Array|اسم المصفوفة `arr` هو `const pointer` بيشاور على أول عنصر.|`arr` هو هو `&arr[0]`.|

**ازاي تفرق بينهم بعينك؟**

- القوسين حوالين النجمة `(*ptr)` ← ده **مؤشر واحد** (لأنه محمي بالأقواس).
    
- مفيش أقواس حوالين النجمة `*arr[]` ← دي **مصفوفة** مؤشرات (الأولوية للـ `[]`).
    

---

### 4. الدوال (Functions & Returns)

|**الشكل (Syntax)**|**المعنى (Meaning)**|**⚠️ خطر جداً**|
|---|---|---|
|`int* func();`|دالة بترجع **عنوان** (Pointer).|إياك ترجع عنوان متغير محلي (`local variable`)، البرنامج هيضرب (Dangling Pointer).|
|`int& func();`|دالة بترجع **Reference** (الأوبجيكت نفسه).|بتستخدم في `Operator Overloading` عشان السلسلة `a=b=c`.|
|`void func(int* p)`|Pass by Address|بتبعت العنوان. الدالة تقدر تغير قيمة المتغير الأصلي. ولازم تستخدم `->` أو `*`.|
|`void func(int& r)`|Pass by Reference|بتبعت "اسم دلع". الدالة بتغير الأصلي بس الشكل أنضف (بدون نجوم).|
|`void func(const int& r)`|Pass by Const Ref|بتبعت الأصل (عشان السرعة) بس ممنوع التعديل (للأمان).|

---

### 5. إدارة الذاكرة (Dynamic Memory Syntax)

| **الأمر**       | **السنتكس**             | **بيعمل إيه؟**                   | **المقابل في المسح**          |
| --------------- | :---------------------- | -------------------------------- | ----------------------------- |
| حجز متغير واحد  | `int* p = new int;`     | بيحجز 4 بايت في الـ Heap.        | `delete p;`                   |
| حجز متغير بقيمة | `int* p = new int(10);` | بيحجز ويحط قيمة 10 جواه.         | `delete p;`                   |
| حجز مصفوفة      | `int* p = new int[5];`  | بيحجز 20 بايت (5 أماكن) ورا بعض. | `delete[] p;` (الأقواس مهمة!) |

---

### نصيحة أخيرة عشان تثبت المعلومة 🧠:

اطبع الجدول ده أو اكتبه في ورقة خارجية وخليها جنبك وأنت بتحل الامتحانات.

كل ما تقابلك * أو & أو const، بص في الجدول وقولي:

"دي تبع انهي صف؟ آه دي Pointer to Const عشان النجمة بعيدة عن الـ Const".

ها يا هندسة، الجدول ده لم الدنيا شوية؟

تحب نختبر الجدول ده بسؤال "مسموم" من أسئلة الامتحانات؟ 😉