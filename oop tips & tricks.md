جمعتلك هنا "الدستور الشامل للـ OOP" 📜.


---

# 🚀 Pro Tips & Tricks for C++ OOP

> [!TIP] القاعدة رقم 0: حدد مكانك
> 
> قبل ما تحل أي سؤال، اسأل نفسك: هل أنا في مرحلة Compile-time (المترجم هو اللي بيقرر) ولا Run-time (البرنامج هو اللي بيقرر)؟

---

## 1. خدعة الـ "Slicing" (القص) 🔪

دي أشهر مصيدة في الامتحانات.

- **السؤال:** دالة بتستقبل باراميتر `Base b`، وبعتنا ليها أوبجيكت `Derived`.
    
- **التريك:** هل الباراميتر مكتوب جنبه `&` أو `*`؟
    
    - ✅ **مكتوب (`Base &b` أو `Base *b`):** زي الفل، **Polymorphism** شغال، وهيتم استدعاء دوال الـ Derived.
        
    - ❌ **مش مكتوب (`Base b`):** حصل **Slicing**. الكومبايلر "قص" الابن وحوله لأب. أي دالة هتتنده هتكون بتاعة الـ **Base** فقط (حتى لو كانت `virtual`).
        

---

## 2. لغز حجم الكلاس (`sizeof`) 📏

لما يجيلك سؤال `sizeof(MyClass)`، احسبها كده:

1. **لو الكلاس فاضي تماماً:** الحجم = **1 Byte** (عشان يكون ليه عنوان مميز).
    
2. **لو فيه `virtual function` (واحدة أو أكتر):** زود حجم مؤشر (`4 bytes` في 32-bit أو `8 bytes` في 64-bit). ده اسمه `vptr`.
    
3. **لو فيه وراثة:** حجم الأب + حجم الابن.
    
4. **الـ Alignment (الحشو):** الكومبايلر بيحب الأرقام الزوجية (مضاعفات 4 أو 8). لو عندك `char` (1 بايت) وراه `int` (4 بايت)، الكومبايلر هيزود 3 بايت حشو (Padding) عشان يظبطهم.
    

> [!EXAMPLE] مثال سريع:
> 
> class A { char c; virtual void f(); };
> 
> - الحساب: 1 (char) + 3 (padding) + 4 (vptr) = **8 bytes**.
>     

---

## 3. قائمة الـ Initialization List (الإجباري) 📝

امتى لازم تستخدم الـ Initializer List : x(val) ومينفعش تعمل Assignment جوه الـ Constructor { x = val; }؟

لو المتغير نوعه حاجة من دول، الكومبايلر هيضرب Error لو مكتبتهاش في القائمة فوق:

1. **Reference:** `int &r;` (لازم يتربط بحاجة فوراً).
    
2. **Const:** `const int c;` (لازم ياخد قيمة فوراً ومينفعش يتعدل بعدين).
    
3. **No Default Constructor:** لو عندك أوبجيكت جوه الكلاس، والكلاس بتاعه مفيهوش Constructor فاضي.
    

---

## 4. تريكة الـ Operators (= و Copy) 🔄

- **قاعدة الـ Big Three:** لو الكلاس بتاعك فيه `Pointer` وبتعمل `new` في الـ Constructor:
    
    - لازم تعمل **Destructor** (عشان `delete`).
        
    - لازم تعمل **Copy Constructor** (Deep Copy).
        
    - لازم تعمل **Copy Assignment Operator** (Deep Copy).
        
- **ليه؟** عشان لو معملتش كده، النسخ الافتراضي (Shallow Copy) هيخلي اتنين أوبجيكت يشاوروا على نفس المكان في الميموري. لما واحد يتمسح، التاني هيعمل Crash (Double Free Error).
    

---

## 5. مين بينادي مين؟ (Constructor/Destructor Order) 🏗️

تخيلها زي "بناء عمارة":

- **البناء (Constructor):** بنبدا من الأساس (Base) ونطلع للدور الأخير (Derived).
    
    - لو فيه كلاسات تانية جوه الكلاس (Member Objects)، بيتبنوا **قبل** ما كود الـ Constructor بتاع الكلاس نفسه يشتغل.
        
- **الهدم (Destructor):** العكس! بنهد الدور الأخير (Derived) الأول، وننزل للأساس (Base).
    

> [!NOTE] نصيحة تتبع:
> 
> اكتب المخرجات في سطر واحد وانت ماشي: Base -> Member -> Derived. وفي الهدم اعكسهم.

---

## 6. الفرق بين `malloc` و `new` (سؤال إنترفيو) 💡

|**الميزة**|**new / delete**|**malloc / free**|
|---|---|---|
|**النوع**|Operator (C++)|Function (C)|
|**Constructor**|✅ بينادي الـ Constructor|❌ بيحجز ميموري بس (غبي)|
|**الحجم**|بيحسبه لوحده (`sizeof` أوتوماتيك)|لازم تديله الحجم بالبايت|
|**الرجوع**|بيرجع مؤشر من النوع الصح|بيرجع `void*` ولازم تعمله Casting|

---

## 7. إزاي تقرأ الـ Pointers المعقدة؟ (قاعدة الحلزونة 🐌)

لو جالك تعريف معقد زي: int *(*x[10])(int);

اقرأ من عند الاسم x واطلع لبره حسب الأولوية:

1. `x[10]`: مصفوفة حجمها 10.
    
2. `*`: مصفوفة مؤشرات.
    
3. `(int)`: مؤشرات لدوال بتاخد int.
    
4. `*`: الدوال دي بترجع مؤشر.
    
5. int: المؤشر اللي راجع ده لنوع int.
    
    النتيجة: مصفوفة مؤشرات لدوال، بترجع مؤشرات لأعداد صحيحة. (متقلقش دي للوحوش بس 😉).
    

---

### 💡 نصيحة أخيرة للامتحان:

في أسئلة الـ **Output Tracing**، لو لقيت كود فيه `virtual`، ارسم جدول صغير جنبك (vtable) واكتب فيه كل دالة بتشاور على مين (الأب ولا الابن). الرسم بيوفر نص الوقت وبيمنع اللخبطة.

ها يا هندسة، حاسس إنك بقيت مسلح أكتر دلوقتي؟ 💪

---

# 🏛️ The Ultimate C++ OOP Cheat Sheet

## 1. قاعدة الميلاد والموت (Life Cycle) 👶💀

> [!abstract] **القاعدة**
> 
> - **البناء (Construction):** بنبني الأساس الأول، ثم الدور اللي فوقه (**Base $\rightarrow$ Derived**).
>     
> - **الهدم (Destruction):** بنهد الدور الأخير الأول، ثم الأساس (**Derived $\rightarrow$ Base**).
>     
> - **ملحوظة:** المتغيرات (Members) بتتخلق قبل ما كود الكونستركتور يتنفذ.
>     

**💻 الكود:**



```C++
#include <iostream>
using namespace std;

class Base {
public:
    Base() { cout << "1. Base Created (Foundation)\n"; }
    ~Base() { cout << "4. Base Destroyed\n"; }
};

class Derived : public Base {
public:
    Derived() { cout << "2. Derived Created (Upper Floor)\n"; }
    ~Derived() { cout << "3. Derived Destroyed\n"; }
};

int main() {
    {
        Derived d; 
    } // هنا الـ Scope خلص، فالهدم هيبدأ بالعكس
    return 0;
}
```

---

## 2. قاعدة الـ Binding (Virtual vs Normal) 🔗

> [!TIP] **القاعدة**
> 
> - **دالة عادية (Non-Virtual):** الكومبايلر أعمى! بيبص على **نوع المؤشر** (`Base*`) وينفذ دالة الأب. (Static Binding).
>     
> - **دالة وهمية (Virtual):** الكومبايلر ناصح! بيبص على **نوع الأوبجيكت الحقيقي** (`Derived`) وينفذ دالة الابن. (Dynamic Binding).
>     

**💻 الكود:**



```C++
#include <iostream>
using namespace std;

class Base {
public:
    void normalFunc() { cout << "Base Normal (Pointer Type)\n"; }
    virtual void virtualFunc() { cout << "Base Virtual\n"; }
};

class Derived : public Base {
public:
    void normalFunc() { cout << "Derived Normal\n"; }
    void virtualFunc() override { cout << "Derived Virtual (Object Type)\n"; }
};

int main() {
    Base* ptr = new Derived(); // مؤشر أب بيشاور على ابن

    ptr->normalFunc();   // هيطبع Base (لأنها مش virtual)
    ptr->virtualFunc();  // هيطبع Derived (لأنها virtual)

    delete ptr;
    return 0;
}
```

---

## 3. قاعدة الـ Virtual Destructor (الأمان) 🛡️

> [!DANGER] القاعدة
> 
> "أي كلاس فيه دالة Virtual، لازم الـ Destructor بتاعه يكون Virtual".
> 
> لو معملتش كده، لما تمسح المؤشر (delete ptr)، هيمسح الأب بس ويسيب الابن في الميموري (Memory Leak).

**💻 الكود:**



```C++
class DangerousBase {
public:
    ~DangerousBase() { cout << "Base Only Deleted! (Leak)\n"; } 
};

class SafeBase {
public:
    virtual ~SafeBase() { cout << "Base Deleted\n"; } 
};

class Derived : public SafeBase {
public:
    ~Derived() { cout << "Derived Deleted First (Clean!)\n"; }
};

int main() {
    SafeBase* ptr = new Derived();
    delete ptr; 
    // النتيجة الصح: Derived Deleted -> Base Deleted
    return 0;
}
```

---

## 4. قاعدة الـ Hiding (خدعة الأسماء) 🙈

> [!WARNING] القاعدة
> 
> لو عرفت دالة في الابن بنفس اسم دالة في الأب (ومش virtual)، الابن بيخفي كل دوال الأب اللي بنفس الاسم (حتى لو الـ Parameters مختلفة).

**💻 الكود:**



```C++
class Base {
public:
    void func() { cout << "Base func()\n"; }
    void func(int x) { cout << "Base func(int)\n"; }
};

class Derived : public Base {
public:
    // Hiding: الدالة دي خفت كل دوال func اللي في الأب
    void func() { cout << "Derived func()\n"; }
};

int main() {
    Derived d;
    d.func();       // شغال
    
    // d.func(10);  // Error! الكومبايلر مش شايف دالة الأب اللي بتاخد int
                    // لأنه حصلها Hiding.
    
    d.Base::func(10); // الحل: لازم تناديها كده
    return 0;
}
```

---

## 5. قاعدة الـ Rule of Three (Deep Copy) 📝

> [!important] القاعدة
> 
> لو الكلاس بتاعك فيه مؤشر (pointer) وبتعمل new في الكونستركتور، لازم تعمل الـ 3 دول عشان تمنع تداخل الذاكرة:
> 
> 1. **Destructor** (عشان `delete`).
>     
> 2. **Copy Constructor** (عشان ينسخ القيمة مش العنوان).
>     
> 3. **Copy Assignment Operator** (عشان `a = b`).
>     

**💻 الكود:**



```C++
class MyClass {
    int* ptr;
public:
    MyClass(int val) { ptr = new int(val); }
    ~MyClass() { delete ptr; } // 1. Destructor

    // 2. Copy Constructor (Deep Copy)
    MyClass(const MyClass& other) {
        ptr = new int(*other.ptr); // مكان جديد + نسخ القيمة
    }

    // 3. Assignment Operator
    MyClass& operator=(const MyClass& other) {
        if (this != &other) {
            delete ptr;
            ptr = new int(*other.ptr);
        }
        return *this;
    }
};
```

---

## 6. المصيدة: Default Arguments in Virtual Functions 🎭

> [!bug] **القاعدة**
> 
> - **جسم الدالة:** بيتحدد وقت التشغيل (Dynamic) $\leftarrow$ دالة الابن.
>     
> - **القيمة الافتراضية:** بتتحدد وقت الترجمة (Static) $\leftarrow$ قيمة الأب.
>     
> - **النتيجة:** دالة الابن هتشتغل، بس هتاخد القيمة الافتراضية بتاعة الأب!
>     

**💻 الكود:**



```C++
class Base {
public:
    virtual void func(int x = 10) { cout << "Base: " << x << endl; }
};

class Derived : public Base {
public:
    void func(int x = 20) override { cout << "Derived: " << x << endl; }
};

int main() {
    Base* ptr = new Derived();
    ptr->func(); 
    // Output: "Derived: 10" 
    // (الدالة من الابن، والرقم من الأب!)
    return 0;
}
```

---

كلمة `static` دي فعلاً "الحرباء" بتاعة C++، لأن معناها بيتغير تماماً حسب مكانها في الكود. عشان كده بتلخبط ناس كتير.

عشان تكسر عقدتها للأبد، قسمها في دماغك لـ **3 سيناريوهات** فقط. كل سيناريو ليه قاعدة ثابتة مش بتتغير.

إليك **"كتالوج الـ Static"** الشامل:

---

### 1. الـ Static جوه دالة (The Immortal Variable) 🧟‍♂️

لما تكتب `static` جوه دالة عادية.

- **السلوك:** المتغير ده بيبقى "مُخلّد". بيتولد أول مرة الدالة تتنده، ويفضل محتفظ بقيمته حتى لما الدالة تخلص وترجع تاني.
    
- **المكان:** مش بيعيش في الـ Stack (زي المتغيرات العادية اللي بتموت)، ده بيعيش في الـ **Data Segment**.
    
- **التريكة في الامتحان:** سطر التعريف `static int x = 0;` بيتنفذ **مرة واحدة بس** في عمر البرنامج كله.
    

**مثال التتبع (Tracing):**

C++

```
void counter() {
    static int count = 0; // السطر ده كأنه مش موجود بعد أول مرة
    count++;
    cout << count << " ";
}

int main() {
    counter(); // طبع 1 (count كان 0 بقى 1)
    counter(); // طبع 2 (count لسه فاكر إنه كان 1)
    counter(); // طبع 3
}
```

---

### 2. الـ Static جوه الكلاس (The Shared Property) 🌐

لما تكتب `static` كـ Data Member جوه كلاس.

- **السلوك:** المتغير ده مش ملك للأوبجيكت، ده **ملك للكلاس نفسه**.
    
- **التشبيه:** زي "باسورد الواي فاي" في الكافيه. كل زبون (Object) ليه مشروبه الخاص، بس كلهم مشترين في نفس الباسورد (Static Variable). لو غيرته، هيتغير عند الكل.
    
- **التريكة القاتلة:**
    
    1. لازم يتعمل له **Definition بره الكلاس** (`int ClassName::var = 0;`) وإلا هيديك Linker Error.
        
    2. **`sizeof(Object)`** ملوش دعوة بالـ static. الـ static مش بيتحسب في حجم الأوبجيكت لأنه في مكان تاني خالص.
        

**مثال:**

C++

```
class User {
public:
    int id;             // عادي (كل يوزر ليه id)
    static int count;   // مشترك (عداد لكل اليوزرز)
};

int User::count = 0;    // لازم السطر ده بره!

int main() {
    User u1, u2;
    u1.count = 5;
    cout << u2.count;   // هيطبع 5 (لأنه نفس المتغير)
}
```

---

### 3. الـ Static Function جوه الكلاس (The Isolated Function) 🏝️

لما دالة جوه الكلاس تكون `static`.

- **السلوك:** دي دالة "يتيمة" ملهاش أب (ملهاش `this` pointer).
    
- **القيد:** بما إن ملهاش `this`، فهي **عمياء** عن أي متغير عادي جوه الكلاس.
    
    - تقدر تشوف الـ Static Variables بس.
        
    - مقدرش تشوف الـ Normal Variables.
        
- **الميزة:** تقدر تناديها من غير ما تعمل أوبجيكت أصلاً (`ClassName::func()`).
    

**التريكة (سؤال امتحان):**

C++

```
class Test {
    int x;
    static int y;
public:
    static void func() {
        y = 10; // صح (static يكلم static)
        // x = 10; // Error! (معنديش this عشان أعرف x بتاعة مين)
    }
};

int main() {
    Test::func(); // بتتنادى كده
    
    Test *ptr = NULL;
    ptr->func(); // تريكة! هتشتغل عادي ومش هتعمل Crash لأنها مش محتاجة this.
}
```

---

### ملخص القواعد الذهبية للـ Static 🌟

|**الحالة**|**المكان**|**السلوك**|**تريكة الامتحان**|
|---|---|---|---|
|**Local Static**|جوه دالة|يحتفظ بقيمته بين الاستدعاءات.|سطر التعريف بيتنفذ مرة واحدة.|
|**Static Member**|جوه كلاس|متغير واحد مشترك بين كل النسخ.|لا يُحسب ضمن `sizeof` ولازم يتعرف بره.|
|**Static Function**|جوه كلاس|دالة بتشتغل بدون أوبجيكت.|ملهاش `this` ومبتشوفش غير الـ static.|

---

### سؤال سريع يثبت المعلومة 🧠

**لو فهمت اللي فوق، جاوب السؤال ده:**

C++

```
class A {
    static int x;
public:
    void set(int v) { x = v; }
    void print() { cout << x; }
};
int A::x = 10;

int main() {
    A a, b;
    a.set(20);
    b.set(30);
    a.print();
}
```

**الناتج إيه؟**

1. 20
    
2. 30
    
3. 10
    
4. Error
    

<details>

<summary>اضغط هنا عشان تشوف الإجابة</summary>

الإجابة هي <b>30</b>.

لأن x مشترك. a خلته 20، وبعدين b خلته 30. لما a جت تطبع، لقت القيمة الأخيرة (30).

</details>